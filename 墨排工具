# 墨排项目归档

更新时间：2026-07-23

## 项目定位

墨排是一个本地运行的微信公众号自助排版工具，用于保持文章格式、视觉风格和发布流程的一致性。

核心目标：

- 粘贴或导入文章内容
- 自动套用统一公众号排版主题
- 实时手机预览
- 一键复制到微信公众号后台
- 支持图片、Word、项目备份和本地文章管理

## 当前实现方式

这是一个纯前端本地工具：

- 无需后端服务
- 无需登录
- 默认运行在 `localhost`
- 数据保存在当前浏览器本地
- 图片和文章库使用 IndexedDB
- 单篇项目可导出为 `.mopai`

## 主要文件

```text
index.html              页面结构
styles.css              工具界面、主题样式、预览样式
app.js                  编辑、解析、复制、存储、Word 导入等核心逻辑
README.md               项目说明
迁移说明.md             跨电脑迁移说明
启动墨排.bat            Windows 一键启动脚本
启动墨排.ps1            PowerShell 启动脚本
vendor/jszip.min.js     DOCX 解析依赖
内容文件/               可选文章备份目录
```

## 已完成功能

### 编辑与预览

- Markdown / 纯文本编辑
- 单行即单段，适合公众号写作习惯
- 实时手机预览
- 手机预览窗口放大至 `430 × 760`
- 正文、标题、引用、列表、代码块、分割线渲染
- 中英文之间多余空格在预览与复制时自动清理
- 字数统计与预计阅读时间

### 主题与样式

- 内置三套主题：
  - 极简白
  - 编辑部
  - 暖手记
- 支持品牌色调整
- 支持正文字号、行距、段距调整
- 支持首行缩进开关
- 支持图片圆角开关

### 图片能力

- 点击按钮插入本地图片
- 支持拖拽图片
- 支持粘贴截图或图片
- 支持 JPG、PNG、WebP、GIF
- 普通图片自动压缩，最长边约 1600px
- GIF 保留动画，限制 4MB
- 图片数据保存到 IndexedDB
- 编辑区只显示 `【图片：图片名称】`
- 编辑区下方显示图片缩略图卡片
- 支持点击缩略图定位正文图片
- 支持删除图片
- 图片下方自动显示说明文字
- 图片说明与图片居中对齐
- 图片说明字体与正文保持一致

### 富文本粘贴

从 Word、飞书、网页等复制富文本时：

- 自动清理原始字体、颜色、背景等冗余样式
- 保留标题、段落、列表、引用、加粗、斜体、链接和图片
- 转换为当前工具的统一排版结构

### Word 导入

支持上传 `.docx` 文件并自动转换为当前排版格式。

可识别：

- 一至三级标题
- 正文段落
- 加粗、斜体
- 链接
- 项目符号列表
- 编号列表
- 引用
- 表格文字
- 文档内常见图片

暂不完整支持：

- 旧版 `.doc`
- 公式
- SmartArt
- 浮动文本框
- EMF/WMF 等旧式图片格式

### 滚动同步

- 编辑区与手机预览支持同步滚动
- 可点击“同步滚动”按钮开关
- 图片较多时使用段落锚点式同步
- 同步依据语义段落位置，而不是单纯滚动百分比
- 图片加载完成后自动重新校准

### 复制到公众号

- 一键复制富文本 HTML
- 同时写入 `text/html` 和 `text/plain`
- 复制前执行发布检查
- 严重错误会阻止复制并打开检查面板

### 项目导入导出

- 单篇文章可导出为 `.mopai`
- `.mopai` 包含：
  - 正文
  - 图片
  - 当前主题
  - 样式设置
  - 滚动同步设置
- 可从 `.mopai` 导入恢复
- 导入前自动创建安全备份
- 文件格式与数值范围会做基础校验

### 文章工作区

通过顶部当前文章名称打开。

支持：

- 新建文章
- 搜索文章
- 重命名文章
- 复制文章
- 切换文章
- 删除文章
- 显示当前文章状态

### 历史版本

- 自动保留最近 20 个历史版本
- 支持手动“立即留档”
- 支持恢复历史版本
- 恢复前会自动保存当前状态

### 发布前检查

目前检查项：

- 是否存在一级标题
- 图片数据是否完整
- 是否有过长段落
- 是否有占位内容
- 外部链接数量提示
- 字数、图片数和阅读时间提示

### 本地存储

- 使用 IndexedDB 保存文章、图片和历史版本
- `localStorage` 只保留轻量索引和兼容数据
- 工作区可查看文章数、版本数、图片数和浏览器存储用量

## 本地运行方式

推荐：

```powershell
python -m http.server 4173 --bind 127.0.0.1
```

然后访问：

```text
http://127.0.0.1:4173
```

Windows 下也可以双击：

```text
启动墨排.bat
```

不推荐直接打开 `index.html`，因为浏览器剪贴板能力在 `localhost` 下更稳定。

## 数据保存位置

当前数据保存在浏览器本地：

- IndexedDB：文章、图片、历史版本
- localStorage：轻量索引和兼容迁移数据

重要提醒：

- 数据只存在当前浏览器、当前设备
- 清理浏览器网站数据会删除文章库
- 换电脑不会自动带过去
- 重要文章建议导出 `.mopai`

## 迁移方式

### 迁移工具

复制整个项目目录即可。

### 迁移文章

旧电脑：

```text
项目 → 导出项目
```

新电脑：

```text
项目 → 导入项目
```

当前版本主要支持单篇 `.mopai` 项目迁移。

## 暂未实现但建议后续优化

优先级较高：

- 一键整库备份与恢复
- 云端同步
- 图片云端存储
- 图片水印
- 图片裁剪、替换、排序和说明编辑
- 真实微信公众号后台兼容测试
- 可视化编辑模式
- 自动化测试
- 文章回收站
- 自定义主题管理

## 云端同步建议方案

当前未接入云。

推荐第一阶段方案：

```text
Supabase Auth
+ Supabase PostgreSQL
+ Supabase Storage
+ IndexedDB 离线缓存
```

如果主要服务中国大陆用户，可考虑：

```text
腾讯云 CloudBase
+ 腾讯云 COS
```

云端同步需要处理：

- 用户登录
- 文章数据同步
- 图片上传
- 历史版本同步
- 多设备冲突合并
- 权限隔离
- 本地离线缓存

## 当前风险

- 真实微信公众号后台兼容性仍需系统测试
- 整库迁移尚未完成
- 图片仍主要保存在本地浏览器
- Word 导入对复杂对象支持有限
- 没有自动化测试保护后续迭代

## 推荐下一步

```text
整库备份与恢复
→ 真实公众号兼容测试
→ 图片管理增强
→ 图片水印
→ 云端同步
```
